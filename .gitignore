# Environment files — never commit real secrets (e.g. NEWS_API_KEY)
.env
.env.local
.env.*.local

# Runtime data and server-only configuration
api/config.local.php
api/data/*.sqlite
api/data/*.sqlite-shm
api/data/*.sqlite-wal
api/uploads/*
!api/uploads/.htaccess
!api/data/.htaccess

