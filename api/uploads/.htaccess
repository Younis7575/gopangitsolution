# Uploaded CVs / attachments must never be downloadable directly.
# They are served only through the API (with the correct headers).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
